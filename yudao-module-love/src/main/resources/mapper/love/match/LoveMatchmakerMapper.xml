<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.love.dal.mysql.match.LoveMatchmakerMapper">

    <select id="selectDefaultMatchmaker"
            resultType="cn.iocoder.yudao.module.love.dal.dataobject.match.LoveMatchmakerDO">
        SELECT id, name, avatar, introduction, mobile, wechat_no, sort,
               is_default, status, creator, create_time, updater, update_time, deleted
        FROM love_matchmaker
        WHERE deleted = b'0'
          AND status = 0
          AND is_default = b'1'
        ORDER BY sort ASC, id ASC
        LIMIT 1
    </select>

</mapper>
